# 이 파일을 .env로 복사 후 값 수정
# cp .env.example .env

# ── 데이터베이스 ──────────────────────────────
DB_HOST=192.168.10.5
DB_PORT=3306
DB_USER=root
DB_PASSWORD=unde5466
DB_NAME=rcs_db

# ── 프론트엔드 API URL ────────────────────────
# 리눅스 서버의 실제 IP 또는 도메인으로 변경
# 예) http://192.168.10.100:8000
NEXT_PUBLIC_API_URL=http://서버IP:8000
