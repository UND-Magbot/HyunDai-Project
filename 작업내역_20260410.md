# 작업 내역 (2026.04.10)

## 1. Convoy 작업 로직 개선

### 도착 검증 (핵심 수정)
- 정지점 도착 검증 임계값: **10cm** (실제 좌표 확인)
- 재이동 모드: **standard → along_given_route(트랙)** 로 변경
- 진입 경로도 동일하게 좌표 검증 추가
- 버튼 누르면 `confirm_event.set()` → 도착 검증 스킵 → 바로 진행

### 모든 이동 무한 재시도
- 진입, 작업 루프, 복귀, 대기지점 이동 전부 무한 재시도
- `_return_robot_simple`(비상정지 후 개별 복귀)도 무한 재시도 추가

### 장애물 처리
- stuck 무한 재시도 (20회마다 로그)
- `stuck_delay`: 1.5s → 2.0s → **2.5s**
- `MOVE_TIMEOUT`: 5분 → **30분**
- `RECOVERY_DELAY`: 3s → **1.5s**

## 2. 태블릿

### 버튼/UI
- 확인 버튼 **미리 표시** (정지점 이동 시 `show_confirm: True`)
- 3초 쿨다운으로 더블클릭 방지
- 배터리 표시: 왼쪽 사이드 중앙 (아이콘 위, % 아래)
- 종료 버튼: `⏻` → `OFF` (폰트 깨짐)
- 폴링: 1초 → **0.5초**
- 장애물 감지 음성 제거

### 일시정지 상태 표시
- `control_mode == manual` 감지 → "일시정지 / 다시 출발 대기 중" 표시

### 명칭 변경
| 변경 전 | 변경 후 |
|---|---|
| 투입 | 피킹 |
| 배출 | 투입 |

### 데모 페이지
- `/api/tasks/tablet/demo` — 실제 태블릿 화면과 동일한 프리뷰

## 3. 비상정지/재개 (일시정지 방식)

### 기존 force_stop → 일시정지로 변경
- `/api/convoy/pause` — 모든 로봇 `control_mode: manual` → 즉시 멈춤
- `/api/convoy/resume` — `control_mode: auto` → 이어서 이동 재개
- convoy 상태 유지, 이동 명령 유지 → 재개 시 바로 이어서

### 비상정지 팝업
- "다시 출발" 버튼만 남김 (전체복귀/다시시작 제거)
- 화재 경보 버튼 제거

## 4. 배터리 스케줄링

### 충전소 예약
- 복귀 로봇에게 **빈 충전소 먼저 예약** (DB `charging_id` 설정)
- 재배치 시 예약된 충전소 제외 → 나머지 로봇은 남은 위치
- 대기 로봇(standby pool) 없으면 배터리 스케줄링 스킵

## 5. 복귀 배너 자동 숨김
- `phase == returning` 이지만 모든 로봇이 `charging/standby/idle` → 배너 자동 숨김
- 시작/종료 버튼도 자동 리셋

## 6. Resume (작업 시작 시)
- 재시작 시 정지점(WORK2/WORK4) **위에 있는 로봇만** 확인 버튼 대기 (30cm 이내)
- 정지점 사이에 있으면 바로 이동

## 7. DB 이중화 (5번 ↔ 6번)

### Master-Master 양방향 복제
- `my.cnf`에 server-id, log-bin, auto-increment-increment, offset 설정
- 5번/6번 서로 repl 계정 생성 + `CHANGE MASTER TO` 연결
- `SHOW SLAVE STATUS` 양쪽 `Yes/Yes` 확인
- 양방향 복제 정상 동작 확인

### DB 풀 확장
- `pool_size`: 10 → 20
- `max_overflow`: 20 → 40
- `pool_recycle`: 3600 → 1800
- `pool_timeout`: 30 → 60

## 8. 프론트엔드 빌드 전략
- 5번용: `NEXT_PUBLIC_API_URL=http://192.168.10.5:8000`
- 6번용: `NEXT_PUBLIC_API_URL=http://192.168.10.6:8000`
- 각자 자기 IP로 빌드 → failover 시 각 서버의 프론트 → 각 서버의 백엔드

## 9. 기타
- 출발 순서 확정: **C1 → C2 → C3 → W1** (15초 간격)
- 확인 대기 graceful_event 체크 추가 (진입 첫 정지점)
- 비상정지 시 `node_positions` 스냅샷 저장 → 재시작 시 정확한 위치
- MCCS 이중화 솔루션 운영 매뉴얼 확인 (Docker 작업 전 그룹 잠금 필요)
