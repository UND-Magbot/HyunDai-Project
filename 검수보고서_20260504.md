# 유앤디 RCS 시스템 — 개발 완료 검수보고서

문서 작성일: 2026-05-04
대상 시스템: 유앤디 RCS (로봇 제어 시스템)
보고 범위: 본 프로젝트에서 자체 개발·구현 완료된 모든 로직

---

## 0. 개요

### 0-1. 시스템 정의
- **목적**: AutoXing AMR 다수를 웹 기반으로 통합 제어·모니터링
- **운영 형태**: 단일 로봇 작업 + 다중 로봇 편대주행(Convoy) 동시 지원
- **외부 연동**: 알티올 WCS, ACS(화재 경보)

### 0-2. 기술 스택
| 영역 | 스택 |
|---|---|
| 백엔드 | FastAPI + SQLAlchemy + MariaDB |
| 프론트엔드 | Next.js 16 + React 19 + Three.js + Tailwind |
| 인프라 | Docker Compose + autoheal + watchdog |
| 통신 | HTTP REST + WebSocket + JWT 인증 |

### 0-3. 본 보고서의 범위
본 보고서는 **이 프로젝트에서 자체 구현한 로직만**을 대상으로 하며, 외부 사양서 충족도 평가와는 별개입니다.

---

## 1. 인증 / 사용자 / 권한

### 1-1. 로그인 인증
- **방식**: ID/비밀번호 → bcrypt 해시 검증 → JWT 토큰 발급
- **검증**: 아이디 정규식(`영문+숫자`), 비밀번호 정규식(`6자+영문+숫자+특수문자`)
- **토큰 처리**: localStorage 저장 → 모든 API 자동 헤더 첨부 → 401 응답 시 자동 로그아웃

### 1-2. 사용자 관리
- 사용자 등록/수정/삭제(소프트 삭제) CRUD
- 본인 비밀번호 변경 (현재 비번 검증 후 새 비번 설정)

### 1-3. 권한 체계
- **역할 2종**: 관리자(role=1) / 일반사용자(role=2)
- **메뉴별 권한**: `UserPermission` 테이블로 사용자×메뉴 ACL
- **사이드바 자동 필터**: 사용자가 접근 가능한 메뉴만 표시
- **관리자 전용 화면**: 메뉴 권한 관리 탭

---

## 2. 로봇 관리

### 2-1. 로봇 등록·관리
- 로봇 기본 정보(이름/SN/IP/MAC/모델) 등록
- 활성/비활성 토글로 상위 명령 수신 제어
- 충전소·대기장소 POI를 로봇에 매핑 (FK)
- WCS 호기 번호(wcs_no) 매핑 — `1→AMR01, 6→AMR06`
- 로봇별 배터리 한계값(min/max) 설정

### 2-2. 실시간 상태 조회
- HTTP `/device/info` + WebSocket `/planning_state`, `/battery_state` 병합
- 16개 워커 스레드 풀로 모든 로봇 병렬 조회
- 상태 5종 판정: `IDLE / EXECUTING / CHARGING / OFFLINE / N/A`
- 오프라인 캐시 60초 → 죽은 로봇 반복 조회 차단
- 태블릿 폴링 시 5초 캐시로 부하 경감

### 2-3. 라이브 상태 동기화
- DB(`robots` 테이블) + 라이브 API 결과를 SN 또는 IP 기준 병합
- 모니터링 화면에 실시간 반영 (2초 폴링)

---

## 3. 작업 (단일 로봇)

### 3-1. 작업 시작 입력
- POI 전체 목록 / 정지점 목록 / 진입 경로 3종 입력
- 로봇별 별도 스레드로 비동기 실행

### 3-2. 진입 경로 처리
- **충전 중인 로봇만** 진입 경로 1회 통과 (충전소 → 작업구역)
- 진입 POI들과 첫 작업 POI를 묶어 한 번에 이동 명령

### 3-3. 구간(Segment) 분할
- POI 목록을 정지점 기준으로 자동 분할
- 정지점에서만 정지·태블릿 확인, 나머지는 통과(`route_coordinates`)
- 잔여 경유점은 다음 루프 첫 구간 앞에 자동 prepend

### 3-4. 무한 반복 루프
- 끝까지 돌면 자동으로 다시 1번 구간부터 반복
- 매 루프마다 활동 로그 기록

### 3-5. 이동 감시 (WebSocket)
- 로봇 `/planning_state` 토픽 구독
- `action_id` 필터링으로 우리가 보낸 이동만 추적 (race 방지)
- 15초 무응답 시 HTTP 폴링으로 폴백
- 장애물 감지 시 태블릿에 빨간 배너 + 음성 알림

### 3-6. 이동 재시도
- 이동 실패 시 최대 3회 재시도
- 1.5초 복구 대기 + WebSocket 재연결 후 재전송
- 3회 다 실패하면 작업 에러 종료

### 3-7. 정지점 태블릿 확인
- 정지점 도착 → `waiting_confirmation` 상태
- 태블릿에 빨간 "✔ 작업 확인" 버튼 표시
- 5초 자동 확인 타이머 (수동 누르거나 자동 진행)

### 3-8. 배터리 부족 자동 충전
- 2회차 루프부터 매 루프 시작 시 배터리 체크
- `min_battery` 이하 → WORK1 우선 복귀 → 진입 경로 역순 → 충전소 도킹
- 도킹포인트 좌표 자동 계산 (충전소 yaw 방향 0.9m 오프셋)
- 모든 로봇이 충전 진입하면 전체 작업 종료

### 3-9. 정지 2종
- **그레이스풀 정지**: 현재 구간 마치고 → WORK1 귀환 → 정지
- **즉시 정지**: 현재 이동 즉시 취소 + 루프 정지

### 3-10. 단발 명령
- 충전소 강제 이동 (BFS로 가장 가까운 WORK→충전소 경로 자동 탐색)
- 복귀 명령 (대기장소→충전소 우선순위, 자리 점유 시 C1→C2→C3→W1 자동 재배정)
- 로봇 전원 종료 (태블릿 OFF 버튼, 확인 모달)

### 3-11. 활성 맵 자동 감지
- POI 이름 중복 방지 — 첫 POI로 가장 최근 활성 맵 ID 자동 결정

---

## 4. 콘보이 (편대주행)

### 4-1. 시작
- 모든 참여 로봇 배터리 병렬 조회
- 배터리 적은 순서대로 자리 배정 (C1 → C2 → C3 → W1)
- 로봇별 별도 스레드로 진입 경로 → 작업 루프 진입

### 4-2. 1열 유지
- 각 로봇의 현재 노드(WORK 인덱스) 메모리 저장
- 다음 노드 이동 전 앞 로봇 노드 위치 확인 후 출발
- 자연스러운 기차 형태 운행

### 4-3. 1시간 주기 배터리 재배치
- 운영 중에도 1시간마다 배터리 재조회
- 배터리 적은 로봇 → 충전소(C1)
- 배터리 많은 로봇 → 작업 루프 투입
- 다운타임 없는 자동 로테이션

### 4-4. 일일 자동 리셋
- 매일 정해진 시각(기본 08:00)에 자동 리셋
- 모든 로봇 완충 가정으로 자리 배정 초기화

### 4-5. 일시정지·재개
- 일시정지 시 모든 로봇 좌표(actual_x/y/ori) DB 저장
- 재개 시 저장된 좌표 기준으로 위치 재설정
- 24시간 경과 시 자동 무효화

### 4-6. 전체 복귀·화재 대피
- 전체 복귀: 모든 로봇 WORK1 → C1/C2/C3 자동 도킹
- 화재 대피: ACS 신호 자동 수신 → 안전 위치로 일제 이동 + 화면 빨간 오버레이

### 4-7. DB 이중화
- 콘보이 런타임 상태(`ConvoyRuntime` 테이블)에 저장
- 백엔드 재시작 후에도 복원 가능

### 4-8. 비상 도구
- 저장 위치 삭제 (재개 좌표 초기화)
- 콘보이 강제 초기화 (전체 phase·메모리·런타임 리셋)

---

## 5. 맵 관리

### 5-1. 계층 관리
- 사업장(Business) → 영역(Area) → 맵(RobotMap) 3계층
- 모두 소프트 삭제(`is_active`) — 기록 보존

### 5-2. SLAM 맵핑
- 로봇과 WebSocket 연결 → 실시간 맵핑 데이터 프론트로 중계
- 맵핑 종료 시 4종 데이터(carto_map / occupancy_grid / image / JSON) 자동 다운로드
- 서버 `/static/maps/` 저장 + DB 메타데이터 기록
- 다른 로봇에 백그라운드 자동 동기화

### 5-3. POI 편집 (5종)
- waypoint(경유) / standby(대기) / charging(충전소) / firewall(방화벽) / virtualwall(가상벽)
- SVG 캔버스에서 마우스 편집
- 화면 좌표(SVG x/y) + 로봇 물리 좌표(world_x/y) 양쪽 저장

### 5-4. 좌표 변환
- 픽셀 ↔ 월드 좌표 자동 변환
- `world_x = (grid_x + 이미지폭/2) × resolution + origin_x`

### 5-5. 경로 그리기
- POI 간 경로(MapLine) 직선/곡선 작성
- 방향성 지정 가능 (forward/backward/bidirectional)

### 5-6. 폴리곤(가상벽)
- 다각형 진입 금지 영역 작성

### 5-7. Undo
- 최대 20단계 편집 이력 보관
- POI/라인/폴리곤 모든 편집 작업 대상

### 5-8. 맵 동기화 (Patch / Full)
- **Patch**: 오버레이(POI·충전소·가상벽)만 복사 — 빠름
- **Full**: SLAM 데이터+이미지+오버레이 전체 — 서비스 재시작 필요

### 5-9. 위치 재조정
- 로봇이 위치를 잃었을 때 충전소/대기장소 좌표로 강제 재인식

### 5-10. 충전소 도킹포인트 자동 생성
- 충전소(type=9) 생성 시 도킹포인트(type=36) 자동 생성
- 충전소에서 yaw 방향 0.9m 오프셋

### 5-11. 로봇 직접 API 프록시
- 로봇 HTTP `/maps` CRUD 프록시
- 맵핑 진행 중 WebSocket 3자 릴레이

---

## 6. 모니터링

### 6-1. 화면 구성
- 좌측: 로봇 목록 + 상태 / 우측: 작업 목록 (실행/완료/오류 탭)
- 중앙: 2D/3D 토글 가능한 맵
- 패널 접기/펴기 (상태 localStorage 저장)

### 6-2. 2D 렌더링 (Canvas)
- HTML5 Canvas — 저사양 PC 친화
- 맵 배경 자동 투명화 (BFS 플러드 필)
- POI 마커, 로봇 위치, 경로선, 방향 화살표, 가상벽 표시

### 6-3. 3D 렌더링 (Three.js)
- 동적 로드 (3D 모드 켤 때만 로드 → 첫 로드 빠름)
- 점유 격자 → 3D 벽 자동 압출
- 로봇 3D 모델 (본체+캐스터+기둥+스크린+LED)
- POI/경로/방화벽/가상벽 3D 렌더

### 6-4. 로봇 LED 색상
- 정상=청록 / 충돌위험=빨강 / 근접위험=주황 / 경고=주황 / 오류=빨강
- 멀리서도 한눈에 상태 파악 가능

### 6-5. 콘보이 제어 UI
- 시작/일시정지/정지/비상정지/대피완료/전체복귀 버튼
- 콘보이 phase 실시간 표시
- 화재 오버레이, 비상정지 오버레이, 복귀 진행 오버레이

### 6-6. 실시간 갱신
- 로봇 상태 2초 폴링
- 작업 상태 2초 폴링
- 알람 30초 폴링
- WebSocket으로 로봇 위치 실시간 수신 (맵 페이지)

---

## 7. 통계 (신규)

### 7-1. 가용성 지표
- **MTTR (평균 복구 시간)** = Σ(복구시각 − 고장시각) / 고장 횟수
- **MTBF (평균 고장 간격)** = Σ(다음 고장시각 − 이전 고장시각) / 횟수
- **가용성** = MTBF / (MTBF + MTTR) × 100
- **고장 횟수**: status=3(에러) 진입 시점 카운트

### 7-2. 가동률 지표
- 로봇별 status 점유 비율 산출 (대기/작업중/충전중/에러/오프라인 5종)
- 색상 막대그래프로 시각화

### 7-3. 데이터 누적 로직
- WCS 30초 보고 사이클에서 status가 변경될 때만 `RobotStatusHistory` 자동 INSERT
- 같은 상태 반복 INSERT 안 함 → 데이터 폭증 방지
- 통계 API: 시작 시점부터 자동 누적

### 7-4. 화면 구성
- KPI 카드 4개 (가용성/MTTR/MTBF/고장횟수)
- 로봇별 가용성 테이블 + 현재 상태 배지
- 로봇별 가동률 막대그래프 + 범례
- 기간 필터 (1일/7일/30일/사용자 지정)

---

## 8. 로그 / 알람

### 8-1. 3종 로그 분리
| 종류 | 무엇을 기록 | 누가 만듦 |
|---|---|---|
| AlarmLog | 오류·경고·알림 | 시스템·코드 곳곳 |
| ActivityLog | 사용자 액션 | API 호출 시 자동 |
| SystemLog | 백엔드 런타임 | 파이썬 logging |

### 8-2. 통합 조회
- 로그 페이지에서 ActivityLog + SystemLog 합쳐서 시간순 표시
- 메시지/타입/날짜 필터 + Excel 다운로드

### 8-3. 알람 팝오버
- 상단 종 아이콘 30초 자동 폴링
- 읽지 않은 알람 빨간 배지 + 네온 깜빡임
- TTS 한국어 음성 자동 재생
- 클릭 시 상세 모달 + 자동 읽음 처리

### 8-4. 글로벌 알림 모달
- AlertContext 전역 컨텍스트 관리
- `showAlert({errorCode, errorType, severity})` 호출 시 화면 모달 + DB 자동 저장
- silent 옵션으로 모달 없이 DB 저장만 가능

### 8-5. 백엔드 로그 파일
- `/var/log/backend/backend.log` 영구 저장
- 50MB×5 로테이션 (최대 250MB)
- uvicorn HTTP 요청 로그 제외 (용량 90% 절감)

---

## 9. 설정

| 탭 | 기능 |
|---|---|
| 메뉴 권한 (관리자) | 사용자별 메뉴 ACL 관리 |
| 비밀번호 변경 | 현재 비번 검증 → 새 비번 설정 |
| DB 백업 | SQL/Excel/ZIP 다운로드 + 서버 경로 저장 |
| 콘보이 설정 | 리셋 시각, 배터리 체크 주기 + 비상 도구 |

---

## 10. DB 백업

- SQL 덤프 다운로드 (`mysqldump` 또는 Python 자동 생성)
- 전체 백업 ZIP (SQL + Excel)
- 서버 디렉터리 탐색 + 경로 저장
- File System Access API로 사용자 폴더 직접 선택
- API 실패 시 Mock SQL 폴백 자동 작동

---

## 11. 외부 시스템 연동

### 11-1. WCS 보고
- 30초 주기 자동 보고
- 메시지: `{ sequence, amrs: [{id, status, position, battery, charging_status}, ...] }`
- 호기 매핑 자동 (`wcs_no` → AMR01 등)

### 11-2. WebSocket 캐시
- 각 로봇에 WS 상시 연결 (`tracked_pose`, `battery_state`)
- 캐시에 저장 → 30초마다 일괄 보고

### 11-3. ACS 화재 경보 수신
- WCS → `/acs/fireinfo` POST
- `alarmCode=1` → 화재 대피 자동 실행
- `alarmCode=0` → 화재 해제 자동 실행

---

## 12. 인프라 / 안정성

### 12-1. 컨테이너 자동 복구
- Docker `restart: unless-stopped`
- autoheal 컨테이너로 backend `/ping` healthcheck 감시 → 죽으면 자동 재시작

### 12-2. backend-watchdog
- backend `/ping` 5초 timeout 시 hang 감지
- 컨테이너 로그 마지막 5000줄 호스트 `/tmp` 백업 (10분 cooldown)
- 재시작은 autoheal에 위임 (역할 분리)
- 24시간마다 30일 이상 hang 로그 + dangling 이미지 자동 정리

### 12-3. 자원 한도
- 백엔드 메모리 4GB 한도 (OOM Killer 보호)
- 스레드/FD 한도 65535
- 스레드 스택 1MB로 축소 (3600개 동시 가능)
- DB 풀 20+40 (최대 60 동시 연결)

### 12-4. 헬스체크 4종
- `/ping` — 가벼운 alive (autoheal용)
- `/health` — DB+콘보이 상태 (이중화·LB용)
- `/debug/health` — 스레드/메모리/FD/DB풀 상세
- `/debug/convoy-workers` — 콘보이 워커 진단

### 12-5. DB 자동 마이그레이션
- 시작 시 DB·테이블 자동 생성
- 컬럼 ALTER 자동 적용
- 메뉴 기본 데이터 자동 시딩

### 12-6. 동시성 제어
- 로봇별 이동 잠금 (`_move_locks`) — 동시 이동 방지
- HTTP Session 재사용 — TCP 연결 풀
- ThreadPoolExecutor 16개 재사용 — 누수 방지

---

## 13. 태블릿 UI

- 로봇별 전용 페이지 (1초 폴링, no-store 캐시)
- AMR 라벨 + 큰 글씨 POI 이름 + 상태별 색상
- 회전 스피너, 배터리 게이지 (3색 단계)
- 빨간 "✔ 작업 확인" 버튼 (5초 자동 타이머)
- 장애물 감지 빨간 배너 + 음성 알림
- 일시정지·오류 표시
- OFF 버튼 (전원 종료, 확인 모달)
- 데모 페이지 (전체 상태 미리보기)

---

## 14. 결론

본 보고서에 정리된 기능은 모두 자체 개발 완료되어 운영 환경에서 동작하는 로직입니다.

### 14-1. 핵심 차별점
- **편대주행(Convoy)**: 다중 로봇 협력 운반 + 배터리 자동 로테이션
- **WebSocket 기반 실시간 제어**: HTTP 폴링 대비 즉각적 응답
- **이중화 설계**: DB 런타임 저장 + autoheal/watchdog 자동 복구
- **사용자 권한 분리**: 메뉴별 세밀한 ACL
- **신규 통계 페이지**: MTTR/MTBF/가용성 자동 산출

### 14-2. 안정성 보장
- 컨테이너 hang 자동 감지·복구
- 메모리 누수 시 다른 서비스 보호 (4GB 한도)
- 30일 로그 자동 정리로 디스크 안정 운영
- 24시간 무인 운영 가능

### 14-3. 향후 확장 가능 영역
- 차량별 누적 이동 거리 추적
- 1일 자동 백업 스케줄러
- DB 복원 UI
- 일반 파라메터 UI 설정 페이지

---

본 시스템은 위 기능 일체를 포함하여 운영 환경에서 정상 동작 중입니다.
