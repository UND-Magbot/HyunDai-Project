# 작업 내역 (2026.03.23)

## 1. 태블릿 앱 (TabletApp)

### WebView 서버 직접 로딩 방식 전환
- 기존: APK 내부 `tablet.html` 파일 로드 (로컬 HTML)
- 변경: 서버 URL 직접 로드 (`/api/tasks/tablet/{robot_id}`)
- 서버 HTML 변경 시 APK 재빌드 없이 즉시 반영

### 태블릿 UI 추가
- **배터리 표시**: 상단 배터리 아이콘 + 숫자(%) 표시
  - 0~1 범위 → 0~100% 자동 변환
  - 20% 이하 빨간색 경고
- **종료 버튼**: 우하단 전원 버튼 → 확인 팝업 → 로봇 전체 전원 종료
  - API: `POST /services/baseboard/shutdown` (target: main_power_supply)
- **홈/설정 버튼**: Android WebView 브릿지로 JS 주입 방식 유지
- **AMR 라벨**: `AMR01` 형태로 zero-padded 표시 (wcs_no 기반)

### 태블릿 데모 페이지
- 경로: `/api/tasks/tablet/demo`
- 모든 상태(대기, 이동, 투입/배출 대기, 대피, 복귀, 충전, 오류 등) 미리보기
- 16:10 태블릿 비율 프리뷰 프레임

---

## 2. Convoy 작업 로직

### 비상정지 후 순차 복귀
- `return_all_convoy(interval=15.0)` — 15초 간격 1대씩 순차 출발
- 중복 호출 방지 플래그 (`_return_all_running`) 추가
- 전체복귀 누르면 비상정지 팝업 닫히고 상단 복귀 배너 표시

### 작업 정밀도 변경
- WORK2/WORK4 정지점 도착 정밀도: `0.05m` → `0.03m`

### 복귀 네비게이션
- ENTER-LAST 경유 시 정지 없이 통과만 하도록 변경

---

## 3. 맵 동기화 개선

### 오버레이 병합 방식
- 기존: 오버레이 전체 교체 → 충전소 POI 사라짐
- 변경: 로봇 기존 오버레이 읽기 → 관리 타입(충전소/도킹/가상벽)만 교체, 나머지 보존
- 관리 타입: type=1(가상벽), type=9(충전소), type=36(도킹포인트)

### `json` import 버그 수정
- `name 'json' is not defined` 오류 — 함수 스코프 내 import 누락 수정

---

## 4. 맵 편집 — 가상벽 기능

### 가상벽(Virtual Wall) 추가
- 새 도구: `virtualwall` (기존 `firewall`과 분리)
- 4점 클릭 방식으로 사각형 폴리곤 생성
- DB 저장: `map_polygons` 테이블 (shape_type="firewall")
- 맵 동기화 시 AutoXing 로봇 오버레이(type=1, LineString)로 변환

### 제한사항
- `along_given_route`(트랙 이동) 모드에서는 가상벽 무시
- `standard`(자율 경로) 모드에서만 가상벽 인식 (화재 대피 등)

---

## 5. 모니터링 화면 성능 개선

### 로봇 위치 업데이트 throttle
- 기존: WS 메시지마다 React 리렌더 (초당 30회)
- 변경: 100ms 버퍼링 후 일괄 업데이트 (초당 10회)
- `poseBufferRef` + `poseFlushTimerRef`로 구현

---

## 6. 서버/인프라

### Docker 타임존 설정
- `docker-compose.yml`에 `TZ=Asia/Seoul` 추가
- convoy 리셋 시간 비교 정상화 (UTC/KST 불일치 해결)

### DB 문자셋 변환 (192.168.0.21)
- `latin1` → `utf8mb4` 변환: convoy_runtime, map_polygons, convoy_configs, convoy_saved_states, user_permissions
- DB 기본 문자셋도 `utf8mb4`로 변경

### Dockerfile pip 타임아웃
- `--default-timeout=300` 추가 (서버 네트워크 느린 환경 대응)

### 21번 서버 Docker 배포
- `.env` DB_HOST 수정 (`host.docker.internal` → `192.168.0.21`)
- 포트 충돌 해결 (기존 uvicorn 프로세스 종료)

### 로컬 개발 포트 변경
- `run_local.ps1`: 8000 → 8001 (서버 Docker와 충돌 방지)
