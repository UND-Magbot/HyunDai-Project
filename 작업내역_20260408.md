# 작업 내역 (2026.04.08)

## 1. 작업 포인트 도착 검증 (핵심 수정)

### 좌표 기반 도착 검증 시스템
- 로봇 `succeeded` 신호를 신뢰하지 않고 **실제 좌표를 매번 확인**
- 정지점(WORK2/WORK4): 15cm 이내, 일반: 50cm 이내여야 도착 인정
- 미도착 시 **standard(자율 경로) 모드로 재이동** — 무한 반복
- 첫 이동은 트랙(along_given_route), 재시도는 standard → 경로 시작점 불일치 문제 해결
- **진입 경로(entry)에도 동일한 검증 추가** — 진입 시에도 좌표 확인

### 모든 이동 무한 재시도
- 재개 이동, 진입 경로, 작업 루프, 복귀 경로, 대기지점 이동 전부 무한 재시도
- 에러로 빠지지 않고 성공할 때까지 반복 (stop/graceful 시에만 탈출)

## 2. 작업 흐름 개선

### 확인 버튼 사전 표시
- 정지점으로 **이동 시작 시** 태블릿에 확인 버튼 즉시 표시
- 이동 중 버튼 누르면 → 도착 후 대기 없이 바로 다음 구간 출발
- 도착 후에도 안 눌렸으면 기존처럼 대기

### 자동 출발 제거
- 태블릿 자동 확인(5초) 제거 → 버튼 직접 눌러야만 출발
- 백엔드도 자동 진행 제거 → 무한 대기

### 장애물 관련
- 장애물 재시도: 10회 제한 → **무한 재시도** (20회마다 로그)
- 장애물 대기 시간: 1.5초 → **2초**
- MOVE_TIMEOUT: 5분 → **30분**

## 3. 명칭 변경

| 변경 전 | 변경 후 |
|---|---|
| 투입 | 피킹 |
| 배출 | 투입 |

- 태블릿, 백엔드 convoy, 데모 페이지 전부 반영

## 4. 태블릿 UI 개선

### 배터리 표시
- 실시간 WS 조회로 변경 (캐시 제거 → 웹과 동일한 값)
- 배터리 아이콘+텍스트를 AMR 라벨 위로 이동
- 아이콘↔텍스트 간격 확대 (반응형: 16~40px)

### 기타
- 종료 버튼 아이콘: `⏻` → `OFF` (폰트 깨짐 해결)
- 장애물 감지 음성 재생 제거 (배너만 표시)
- 폴링 간격: 1초 → **0.5초** (버튼 더 빠르게 표시)
- 태블릿 데모 페이지 (/tablet/demo) 추가

## 5. Convoy 설정

### 출발 순서
- C1 → C2 → C3 → W1 (15초 간격)

### 배터리 스케줄링
- **대기 로봇(standby pool)이 있을 때만** 배터리 교체 실행
- 4대만 작업 중일 때는 스킵

### 비상정지 후 복귀
- 중복 호출 방지 플래그 추가
- 15초 간격 순차 출발

## 6. 맵 동기화

### 오버레이 병합
- `json` import 버그 수정 (`name 'json' is not defined`)
- 기존 오버레이 읽기 → 관리 타입만 교체, 나머지 보존

## 7. 서버/인프라

### DB 문자셋 (192.168.0.21)
- `latin1` → `utf8mb4` 변환 (convoy_runtime 등 5개 테이블)

### 프론트엔드 배포
- `.env`의 `NEXT_PUBLIC_API_URL` → `http://192.168.10.5:8000` 로 재빌드
- Docker 네트워크: `host.docker.internal` ↔ `172.17.0.1` 설정

### VIP 이중화 준비
- VIP: 192.168.10.7 (5번 Active, 6번 Standby)
- 업체 세팅 후 동일 이미지 배포 예정
